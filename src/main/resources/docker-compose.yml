version: "3.9"

services:
  meeting-scheduler:
    build: .
    container_name: meeting-scheduler
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: default
    restart: unless-stopped